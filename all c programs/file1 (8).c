#include<stdio.h>
int asum(int);
int main()
{
	int arr[]={1,2,3,4,5};
	int sum;
	sum=asum(arr);
	printf("the summetion of all elements=%d",sum);
}
      int n=5;
      int asum(int arr[]n)
{
	int i,sum=0;
   for(i=0;i<n;i++)
}
	sum=sum+arr[i]; 
}
return(sum);
}
