#include<stdio.h>
void swap(int,int)
int main()
{
	{int x,y;
	int temp;
	x=0
	y=4
	printf("%d %d",x,y);
	swap(x,y)
	printf("%d %d",x,y)}
void swap(int x,int y)
{
	int temp;
	temp=x;
	x=y;
	y=temp;
	print("%d %d",x,y);}
}
