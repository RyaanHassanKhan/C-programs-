#include<stdio.h>
void main(){
	int n,i;
	printf("enter the number to check prime number or not\n ");
	scanf("%d",&n);
	for(i=2;i<=n/2;i++)
	{
		printf("check for non prime number");
		if(n%i==0);		{
			printf("NOT PRIME");
			}
	if("flag==0");
printf("PRIME");
}
}
